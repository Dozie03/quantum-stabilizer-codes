{
  "name": "quantum-optimizer-visualizer",
  "version": "0.1.0",
  "private": true,
  "dependencies": {
    "axios": "^1.7.7",
    "react-scripts": "^5.0.1",
    "recharts": "^2.13.3"
  },
  "proxy": "http://127.0.0.1:5000",
  "scripts": {
    "start": "react-scripts start",
    "build": "react-scripts build",
    "test": "react-scripts test",
    "eject": "react-scripts eject",
    "start-api": "cd server && python app.py",
    "dev": "concurrently \"npm run start\" \"npm run start-api\""
  },
  "devDependencies": {
    "concurrently": "^9.1.0"
  },
  "browserslist": {
    "production": [
      ">0.2%",
      "not dead",
      "not op_mini all"
    ],
    "development": [
      "last 1 chrome version",
      "last 1 firefox version",
      "last 1 safari version"
    ]
  }
}
